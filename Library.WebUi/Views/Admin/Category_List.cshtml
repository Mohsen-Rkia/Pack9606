@model IEnumerable<Library.Model.Category>
@{
    ViewBag.Title = "لیست دسته ها";
}

<h2>لیست دسته ها</h2>

<table class="table table-hover table-bordered table-striped">
    <thead>
        <tr>
            <th>شماره دسته</th>
            <th>نام دسته</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var category in Model)
        {
            <tr>
                <td>@category.Id</td>
                <td>@category.Category_Name</td>
            </tr>
        }
    </tbody>

</table>

